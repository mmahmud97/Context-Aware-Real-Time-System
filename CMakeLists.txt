cmake_minimum_required(VERSION 3.10)

project(ContextAwareRealTimeSystem)

# Add subdirectories for dynamic memory storage or other components
add_subdirectory(dynamic_memory_storage)

# Add other components or source files as necessary
